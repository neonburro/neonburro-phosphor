# phosphor.neonburro.com

The burrow. A room for people who hold NEONBURRO, opened by a wallet
signature, gated by a balance the database knows. Built by neonburro in
Ridgway, Colorado. `CLAUDE.md` is the real documentation.

yarn, then `yarn dev`. The door renders without env, the signature needs the
Supabase Web3 provider enabled and the function needs a keyed Solana rpc.
